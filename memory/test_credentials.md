# Test Credentials

No authentication implemented yet. All pages and API endpoints are public.

## API Endpoints (no auth required)
- POST /api/contact — public
- POST /api/audit — public
- POST /api/newsletter — public
- GET /api/leads — public
- GET /api/newsletter/subscribers — public
